# Supporting project files

Snapshot of the research, curriculum sources, graph extraction, design references and rendering scripts from the parent workspace. The application lives at the repository root; the Pi tutor is in `studio/`. Historical notes may contain local paths or refer to the original parent-workspace layout. Generated caches, local credentials, learner records and temporary outputs are excluded.
